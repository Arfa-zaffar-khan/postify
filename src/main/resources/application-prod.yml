server:
  port: 8083
  servlet:
    context-path: /api/${spring.application.name}

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/postify
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect